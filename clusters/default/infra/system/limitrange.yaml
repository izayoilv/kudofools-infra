apiVersion: v1
kind: LimitRange
metadata:
  name: openbao-limits
  namespace: openbao
  labels:
    app.kubernetes.io/part-of: kudofools
    app.kubernetes.io/managed-by: flux
spec:
  limits:
    - default:
        cpu: "500m"
        memory: 512Mi
      defaultRequest:
        cpu: "100m"
        memory: 64Mi
      max:
        cpu: "1"
        memory: 1Gi
      type: Container
---
apiVersion: v1
kind: LimitRange
metadata:
  name: forgejo-limits
  namespace: forgejo
  labels:
    app.kubernetes.io/part-of: kudofools
    app.kubernetes.io/managed-by: flux
spec:
  limits:
    - default:
        cpu: "500m"
        memory: 512Mi
      defaultRequest:
        cpu: "100m"
        memory: 128Mi
      max:
        cpu: "2"
        memory: 2Gi
      type: Container
---
apiVersion: v1
kind: LimitRange
metadata:
  name: woodpecker-limits
  namespace: woodpecker
  labels:
    app.kubernetes.io/part-of: kudofools
    app.kubernetes.io/managed-by: flux
spec:
  limits:
    - default:
        cpu: "300m"
        memory: 256Mi
      defaultRequest:
        cpu: "100m"
        memory: 128Mi
      max:
        cpu: "1"
        memory: 1Gi
      type: Container
---
apiVersion: v1
kind: LimitRange
metadata:
  name: buildkitd-limits
  namespace: buildkitd
  labels:
    app.kubernetes.io/part-of: kudofools
    app.kubernetes.io/managed-by: flux
spec:
  limits:
    - default:
        cpu: "1"
        memory: 1Gi
      defaultRequest:
        cpu: "500m"
        memory: 512Mi
      max:
        cpu: "3"
        memory: 3Gi
      type: Container
---
apiVersion: v1
kind: LimitRange
metadata:
  name: step-pod-limits
  namespace: woodpecker-pipelines
  labels:
    app.kubernetes.io/part-of: kudofools
    app.kubernetes.io/managed-by: flux
spec:
  limits:
    - default:
        cpu: "1"
        memory: 1Gi
      defaultRequest:
        cpu: "200m"
        memory: 256Mi
      max:
        cpu: "3"
        memory: 3Gi
      type: Container
